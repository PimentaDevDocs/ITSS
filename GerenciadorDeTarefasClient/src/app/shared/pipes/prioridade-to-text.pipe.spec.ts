import { PrioridadeToTextPipe } from './prioridade-to-text.pipe';

describe('PrioridadeToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new PrioridadeToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
